<!DOCTYPE html>
<html>
  <head>
    <title>ROR Demo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <% flash.each_with_index do |(name, msg), idx| %>
      <div id="flash_div_#{idx}" class="absolute z-10 top-0 left-0 w-screen h-screen flex items-center justify-center bg-[rgba(0,0,0,0.5)]">
        <p class="relative bg-gray-100 border-gray-400 shadow-2xl w-[500px] px-4 py-3 rounded font-medium">
          <%= msg %>
          <button class="absolute -top-3 -right-3 bg-white p-2 rounded-full shadow-xl" onclick="document.getElementById('flash_div_#{idx}').remove()">
            <svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" class="w-3 h-3" viewBox="0 0 16 16">
              <path d="M2.146 2.854a.5.5 0 1 1 .708-.708L8 7.293l5.146-5.147a.5.5 0 0 1 .708.708L8.707 8l5.147 5.146a.5.5 0 0 1-.708.708L8 8.707l-5.146 5.147a.5.5 0 0 1-.708-.708L7.293 8z"/>
            </svg>
          </button>
        </p>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>